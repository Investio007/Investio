# Point the Investio Railway service at the FastAPI backend (not the Vite frontend).
# In Railway dashboard: Service → Settings → set Root Directory to "server".
# Deploy config for the API lives in server/railway.toml (read when root dir is server/).
